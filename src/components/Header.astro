---
---

<header id="mainHeader" class="main-header">
  <!-- Top Alert Banner -->
  <div class="alert-banner">
    <div class="alert-content">
      <span>🔥</span>
      <span>LIMITED TIME: 70% OFF + FREE WORLDWIDE SHIPPING</span>
      <span>🔥</span>
    </div>
  </div>

  <nav class="nav-container">
    <div class="nav-content">
      
      <!-- Logo (LEFT SIDE) -->
      <a href="https://purefocuslife.fun/" class="logo-section">
        <div class="logo-icon">
          <span class="brain-icon">🧠</span>
          <div class="check-badge">✓</div>
        </div>
        <div class="logo-text">
          <div class="logo-title">Pineal Guardian</div>
          <div class="logo-subtitle">Memory Enhancement Revolution</div>
        </div>
      </a>
      
      <!-- Desktop Navigation (RIGHT SIDE) -->
      <div class="desktop-nav">
        <ul class="nav-links">
          <li>
            <a href="https://purefocuslife.fun/">🏠 Home</a>
          </li>
          <li>
            <a href="https://purefocuslife.fun/blog/">📝 Blog</a>
          </li>
          <li>
            <a href="https://purefocuslife.fun/contact/">✉️ Contact</a>
          </li>
        </ul>

        <!-- Trust Indicators -->
        <div class="trust-indicators">
          <div class="trust-item">
            <div class="stars">⭐⭐⭐⭐⭐</div>
            <div class="trust-label">4.9/5 Rating</div>
          </div>
          <div class="trust-item">
            <div class="trust-number">49K+</div>
            <div class="trust-label">Happy Customers</div>
          </div>
        </div>

        <!-- CTA Button (FAR RIGHT) -->
        <a href="https://pinealguardianvip.com/ds/indexts.php#aff=Graham05" 
           target="_blank" rel="noopener noreferrer"
           class="cta-button">
          🚀 Get Started
        </a>
      </div>
      
      <!-- Mobile Menu Button -->
      <button class="mobile-menu-btn" onclick="toggleMobileMenu()">
        <span id="menuIcon">☰</span>
      </button>
    </div>
    
    <!-- Mobile Navigation -->
    <div id="mobileMenu" class="mobile-nav hidden">
      <div class="mobile-nav-content">
        <ul class="mobile-links">
          <li>
            <a href="https://purefocuslife.fun/">🏠 Home</a>
          </li>
          <li>
            <a href="https://purefocuslife.fun/blog/">📝 Blog</a>
          </li>
          <li>
            <a href="https://purefocuslife.fun/contact/">✉️ Contact</a>
          </li>
        </ul>

        <div class="mobile-trust">
          <div class="mobile-trust-grid">
            <div class="trust-item">
              <div class="stars">⭐⭐⭐⭐⭐</div>
              <div class="trust-label">4.9/5 Rating</div>
            </div>
            <div class="trust-item">
              <div class="trust-number">49K+</div>
              <div class="trust-label">Customers</div>
            </div>
          </div>
        </div>

        <div class="mobile-cta">
          <a href="https://pinealguardianvip.com/ds/indexts.php#aff=Graham05" 
             target="_blank" rel="noopener noreferrer"
             class="mobile-cta-button">
            🧠 Transform My Memory Now
          </a>
        </div>
      </div>
    </div>
  </nav>

  <div class="gradient-border"></div>
</header>

<!-- Spacer -->
<div style="height: 140px;"></div>

<script>
function toggleMobileMenu() {
  const mobileMenu = document.getElementById('mobileMenu');
  const menuIcon = document.getElementById('menuIcon');
  
  if (mobileMenu.classList.contains('hidden')) {
    mobileMenu.classList.remove('hidden');
    menuIcon.textContent = '✕';
  } else {
    mobileMenu.classList.add('hidden');
    menuIcon.textContent = '☰';
  }
}

// Close mobile menu when clicking outside
document.addEventListener('click', function(event) {
  const mobileMenu = document.getElementById('mobileMenu');
  const menuButton = document.querySelector('.mobile-menu-btn');
  
  if (menuButton && !menuButton.contains(event.target) && !mobileMenu.contains(event.target)) {
    mobileMenu.classList.add('hidden');
    document.getElementById('menuIcon').textContent = '☰';
  }
});
</script>

<style>
  /* Main Header */
  .main-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(12px);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    border-bottom: 1px solid rgba(0, 0, 0, 0.05);
  }

  /* Alert Banner */
  .alert-banner {
    background: linear-gradient(135deg, #ef4444, #f97316, #ef4444);
    color: white;
    padding: 10px;
    text-align: center;
  }

  .alert-content {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    font-weight: bold;
    font-size: 14px;
  }

  /* Navigation Container */
  .nav-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 15px 30px;
  }

  .nav-content {
    display: flex;
    justify-content: space-between; /* Logo left, Nav right */
    align-items: center;
  }

  /* Logo Section (LEFT) */
  .logo-section {
    display: flex;
    align-items: center;
    gap: 15px;
    text-decoration: none;
    order: 1; /* Ensure it's first */
  }

  .logo-icon {
    position: relative;
    width: 56px;
    height: 56px;
    background: linear-gradient(135deg, #9333ea, #3b82f6, #10b981);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 10px 30px rgba(147, 51, 234, 0.3);
  }

  .brain-icon {
    font-size: 28px;
  }

  .check-badge {
    position: absolute;
    top: -8px;
    right: -8px;
    width: 24px;
    height: 24px;
    background: #10b981;
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 12px;
    font-weight: bold;
  }

  .logo-text {
    display: flex;
    flex-direction: column;
  }

  .logo-title {
    font-size: 24px;
    font-weight: 900;
    background: linear-gradient(135deg, #9333ea, #3b82f6, #10b981);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .logo-subtitle {
    font-size: 12px;
    color: #10b981;
    font-weight: 600;
  }

  /* Desktop Navigation (RIGHT) */
  .desktop-nav {
    display: flex;
    align-items: center;
    gap: 30px;
    order: 2; /* Ensure it's last */
    margin-left: auto; /* Push to the right */
  }

  .nav-links {
    display: flex;
    gap: 5px;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-links a {
    color: #374151;
    text-decoration: none;
    font-weight: 600;
    padding: 10px 15px;
    border-radius: 8px;
    transition: all 0.3s;
    white-space: nowrap;
  }

  .nav-links a:hover {
    color: #3b82f6;
    background: rgba(59, 130, 246, 0.1);
  }

  /* Trust Indicators */
  .trust-indicators {
    display: flex;
    gap: 20px;
    padding-left: 20px;
    border-left: 2px solid #e5e7eb;
  }

  .trust-item {
    text-align: center;
  }

  .stars {
    color: #fbbf24;
    font-size: 14px;
    line-height: 1;
  }

  .trust-number {
    color: #10b981;
    font-weight: bold;
    font-size: 18px;
    line-height: 1;
  }

  .trust-label {
    font-size: 11px;
    color: #6b7280;
    font-weight: 600;
    margin-top: 2px;
  }

  /* CTA Button (FAR RIGHT) */
  .cta-button {
    background: linear-gradient(135deg, #10b981, #3b82f6, #9333ea);
    color: white;
    padding: 12px 30px;
    border-radius: 25px;
    font-weight: bold;
    text-decoration: none;
    box-shadow: 0 10px 30px rgba(59, 130, 246, 0.3);
    transition: all 0.3s;
    display: inline-block;
    white-space: nowrap;
  }

  .cta-button:hover {
    transform: scale(1.05);
    box-shadow: 0 15px 40px rgba(59, 130, 246, 0.4);
  }

  /* Mobile Menu Button */
  .mobile-menu-btn {
    display: none;
    background: none;
    border: none;
    font-size: 28px;
    cursor: pointer;
    color: #374151;
    order: 3;
  }

  /* Mobile Navigation */
  .mobile-nav {
    margin-top: 20px;
    padding-bottom: 20px;
  }

  .mobile-nav-content {
    background: white;
    border-radius: 15px;
    padding: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .mobile-links {
    list-style: none;
    padding: 0;
    margin: 0 0 20px 0;
  }

  .mobile-links li {
    margin-bottom: 10px;
  }

  .mobile-links a {
    display: block;
    padding: 15px;
    color: #374151;
    text-decoration: none;
    font-weight: 600;
    border-radius: 10px;
    transition: all 0.3s;
  }

  .mobile-links a:hover {
    background: rgba(59, 130, 246, 0.1);
    color: #3b82f6;
  }

  .mobile-trust {
    border-top: 1px solid #e5e7eb;
    border-bottom: 1px solid #e5e7eb;
    padding: 15px 0;
    margin-bottom: 20px;
  }

  .mobile-trust-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 15px;
  }

  .mobile-cta-button {
    display: block;
    width: 100%;
    background: linear-gradient(135deg, #10b981, #3b82f6, #9333ea);
    color: white;
    padding: 15px;
    border-radius: 12px;
    font-weight: bold;
    text-align: center;
    text-decoration: none;
    box-shadow: 0 10px 30px rgba(59, 130, 246, 0.3);
  }

  .hidden {
    display: none;
  }

  /* Gradient Border */
  .gradient-border {
    height: 3px;
    background: linear-gradient(90deg, #9333ea, #3b82f6, #10b981);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .desktop-nav {
      display: none;
    }

    .mobile-menu-btn {
      display: block;
    }

    .logo-subtitle {
      display: none;
    }
  }

  @media (max-width: 768px) {
    .nav-container {
      padding: 15px 20px;
    }

    .logo-title {
      font-size: 20px;
    }

    .logo-icon {
      width: 48px;
      height: 48px;
    }

    .brain-icon {
      font-size: 24px;
    }

    .nav-content {
      gap: 15px;
    }
  }
</style>